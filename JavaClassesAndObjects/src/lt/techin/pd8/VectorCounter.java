package lt.techin.pd8;

public class VectorCounter {


    public double VectorLength(int x, int y, int z){
        double vectorLength = Math.sqrt(x*x +y*y + z*z);

        return vectorLength;
    }
}
